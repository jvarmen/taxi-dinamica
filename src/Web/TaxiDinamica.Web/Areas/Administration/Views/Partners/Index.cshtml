@model TaxiDinamica.Web.ViewModels.Partners.PartnersListViewModel
@{
    this.ViewData["Title"] = "Taxis";
}

<h2>
    Todos las Taxis (@Model.Partners.Count())
    <span>
        <a class="btn btn-info" asp-area="Administration" asp-controller="Partners" asp-action="AddPartner">Añadir Nuevo
            Taxi</a>
    </span>
</h2>

<table class="table table-bordered table-striped">
    <thead class="thead-light">
        <tr>
            <th scope="col">Placa</th>
            <th scope="col">Logo</th>
            <th scope="col">Categoría</th>
            <th scope="col">Conductor</th>
            <th scope="col">Sitio Web</th>
            <th scope="col">Citas</th>
            <th scope="col">Botón de borrar</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var Partner in this.Model.Partners)
        {
            <tr>
                <th scope="row">@Partner.Placa</th>
                <td>
                    <img class="align-self-center" src="@Partner.ImageUrl" style="width:180px;height:120px;">
                </td>
                <td>@Partner.CategoryName</td>
                <td>@Partner.DriverName</td>
                <td>@Partner.DriverContact</td>
                <td>@Partner.AppointmentsCount</td>
                <td>
                    <form method="post">
                        <button type="submit" class="btn btn-danger btn-sm" asp-area="Administration"
                        asp-controller="Partners" asp-action="DeletePartner" asp-route-id="@Partner.Id">
                            Borrar
                        </button>
                    </form>
                </td>
            </tr>
        }
    </tbody>
</table>
